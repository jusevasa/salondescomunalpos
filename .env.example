# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Service Role Key (Required for admin operations like creating users)
# ⚠️ IMPORTANT: This key has admin privileges. Keep it secure and never expose it in client-side code.
# Only use this in server-side operations or admin functions.
VITE_SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Print API Configuration
VITE_PRINT_API_URL=http://localhost:8080